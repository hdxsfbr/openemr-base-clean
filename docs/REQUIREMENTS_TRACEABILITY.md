# Requirements Traceability

This matrix maps the AgentForge PRD to planned implementation and evaluator
evidence. Update the status and evidence links as work lands. A requirement is
not complete merely because code exists; its proof must also be accessible.

Status values: `Not started`, `In progress`, `Verified`, or `Blocked`.

| Requirement | Planned implementation or artifact | Verification evidence | Status |
| --- | --- | --- | --- |
| OpenEMR runs locally with realistic sample data | Development compose stack plus documented clinical demo cohort | `SETUP.md`; repeatable seed command; screenshots or smoke test | Verified locally 2026-09-14 — bundled demo dataset plus deterministic synthetic cohort `af-cohort-v1` (`evals/fixtures/cohort/`: 26 patients, 50 encounters, 134 lab results, 67 notes; identical checksums across reloads; post-load checks pass). Loaded on the deployment by the one-shot `demo-seed` job since 2026-09-15 (26 patients, post-load checks passing; `docs/deployment/digitalocean.md` "Manual Cycle"); re-seeded 2026-09-20 on one UTC clock after the clinic-timezone change was reverted (26 cohort patients and 15 appointments that day, read back in `docs/audit/evidence/performance/brief-on-open-2026-09-20.md` §10) |
| Publicly accessible deployment | Single-Droplet Terraform, restricted Compose networks, and Caddy TLS ingress | `docs/deployment/digitalocean.md`; external smoke test | Verified 2026-09-15 — `v0.1.0-skeleton` live at `https://openemr-137-184-4-22.sslip.io`: project image with the module, agent service, deny-by-default edge (probe: 20 sensitive paths 404), demo users and 26-patient cohort seeded, panel rendering on cohort charts. Kept up during build days; `v0.2.0-slice` and later builds were deployed to the same hostname with `deploy.sh`, and every eval report in `evals/results/` (2026-09-16) and the manual CI job `test:evals-live` target it. Since 2026-09-20 commit `478f432` is live there (`README.md` "Deployment"; `docs/deployment/digitalocean.md` "Current Deployment"; agent `0.3.0` on `/health`, module `0.5.0` as `module_version` from `session.php`); the submission tag `week1-final` differs from it only in docs and eval results, and `evals/results/2026-09-20T064022Z-4d2a9fd.md` ran all 48 cases against that tree (48 passed, every blocking gate PASS). The edge re-probe `docs/audit/evidence/security/cloud-probe-2026-09-20-final.txt` and the Droplet snapshot were taken earlier the same day, before the last redeploy for the alert fixes, which did not change the Caddyfile; the running-image digests were not re-recorded after it. The hostname is still the disposable `sslip.io` one; an owned hostname was not done for Week 1 |
| Backup, restore, rollback and clean deploy | `infra/digitalocean/backup.sh` (encrypted archive: database dump, `openemr_sites` and `agent_state` volumes, secrets and `.env`, under `~/.config/agentforge/backups/`), `restore.sh` (secrets first, database volume re-initialised from them, dump loaded, volumes replaced), rollback by redeploying tag `week1` from a worktree, rehearsal runbook with a timing table in `docs/deployment/digitalocean.md` | Rehearsal timings and the golden run on the rehearsal host (M4) | Verified 2026-09-18 — rehearsed end to end on a throwaway `s-2vcpu-4gb` (`146.190.154.222`) in its own Terraform workspace, never against the live host; timings table in `docs/deployment/digitalocean.md` "Rehearsal Runbook": clean deploy 2m36s, demo-seed plus a 14/14 golden run 3m10s, `backup.sh` 15s, rollback to tag `week1` 2m36s (14/14 golden there too), roll forward 1m13s, `restore.sh` about 3m19s, destroy about 25s. The restore was shown to restore rather than no-op: a conversation created after the backup came back `Unknown conversation.` afterwards. Found and fixed on the way: `push-secrets.sh` failing silently inside `deploy.sh`, which had deployed once with `llm_provider` and `tracer` unconfigured (it now retries and reports), a `printf` option bug that stopped `restore.sh` before its confirmation prompt, and `deploy.sh`'s 180 s bootstrap wait timing out while the operator's SSH identity was not resolving (fixed in the operator's SSH config, not the script). Migration is not applicable: the co-pilot only reads the chart, and its one table, `copilot_conversation` (identifiers only, the module's `sql/install.sql`), has had a single schema version since the skeleton. Before that (2026-09-17) the scripts and runbook were written and nothing had run against a host |
| Full audit with approximately 500-word summary | Security, performance, architecture, data-quality, and compliance passes | `AUDIT.md` (500-word summary; one section per PRD area with every finding; remediation; plan changes; residual risk); detail and evidence in `docs/audit/` | Complete, owner-reviewed 2026-09-14 (Stage 3 gate passed) — all five areas audited on 2026-09-14 with live ACL tests, lead spot-checks, an 8-minute public cloud-window probe, and synthetic-cohort measurements. Authenticated Droplet latency and load tests were later deliverables at that date; both landed 2026-09-18 (baseline and load-test rows below) |
| Narrow target user and concrete workflow | Primary-care physician pre-visit workflow | `USERS.md`; user/use-case review | Verified (document) 2026-09-15, owner-approved — 90-second workflow moment (T−30 s to T+90 s), refusals, the per-chart-versus-schedule-sweep decision (sweep deferred as UC-04), rejected and scheduled use cases, and the CAP-01..08 capability table `ARCHITECTURE.md` traces to. Clinician-proxy validation (six checkboxes) still open and tracked in `USERS.md` |
| Every use case explains why an agent is appropriate | Explicit rationale and rejected UI alternatives per use case | `USERS.md` use-case sections | Verified 2026-09-15 — each of UC-01..03 carries a widget / sorted list / chart view / agent comparison, an "honest limit" stating where a non-agent would suffice, and a worked synthetic example; owner-approved. The clinician interview may revise wording |
| Architecture plan with approximately 500-word summary | Audit-informed context, components, trust boundaries, and tradeoffs | `ARCHITECTURE.md`; ADRs | Verified 2026-09-15 (Stage 5 gate passed) — revised against `AUDIT.md` §8 and ADR-0002/0003: 594-word summary, request path, per-turn delegation, CAP-01..08 traceability, tool matrix, contracts, LangGraph turn graph, checkpointed state, verifier, failure matrix, observability, API and Bruno collection design, deployment, Weeks 2–3 compounding. ADR-0004..0007 accepted 2026-09-15; dated amendments since: ADR-0004 (one plan round, 2026-09-18) and, on 2026-09-19, ADR-0003 (brief on chart open, module 0.5.0), ADR-0006 decision 7 (summary grounding and fallback) and ADR-0007 (content capture mode) |
| Key metrics with rationale | Safety, utility, latency, reliability, and cost metrics | `KEY_METRICS.md`; dashboard/eval links | Verified (definitions) 2026-09-15 — metrics, gaming defenses, decision thresholds (release gates, the three PRD alerts with responses, risk-acceptance rules) owner-approved; latency thresholds still marked provisional in `KEY_METRICS.md` (the load baseline was recorded 2026-09-18; the clinician interview is open); the cost gate was configured 2026-09-17 against the $0.0223 projection; a "Physician wait for the brief" row was added 2026-09-19 with module 0.5.0 (`evals/brief_latency.py`). Eval evidence is the release-gate table that opens each run report in `evals/results/` (all but the first, 2026-09-16); dashboard evidence is in the dashboard row below |
| Embedded agentic chatbot | OpenEMR patient-dashboard module | Multi-turn live demo and UI tests | Verified 2026-09-16 — chat transcript panel on the patient dashboard: summary paragraph, claims table with chart links, follow-up chips from the turn's records, live progress over server-sent events, history restored behind a fresh ticket, 30-minute idle close. Full UC-01/02/03 turns with Sonnet 5 in 12 to 28 s on the deployment; eval run 2026-09-16 (`evals/results/`) exercises the same path headlessly. Since 2026-09-19 (module 0.5.0, ADR-0003 amendment) the panel starts the UC-01 brief itself when the chart finishes loading, through the same session, ticket, ACL and audit path as a click; `BriefPolicy` decides server-side from `COPILOT_BRIEF_ON_OPEN` (code default `visit_today`, the demo Droplet's compose overrides to `always`, an unknown value fails closed to `off`; never for a break-glass login or a role with no clinical section). Physician wait measured by `evals/brief_latency.py` at 2026-09-20T03:04Z (12 briefs on four charts, `docs/audit/evidence/performance/brief-latency-2026-09-19.md`): chart open to a verified brief p50 13.4 s, p95 17.5 s; wait left after a 10 s reading lag p50 3.4 s, p95 7.5 s, against 12.7 s and 16.9 s for the same turns counted as the old click flow. The lag is a parameter, not an observed physician session, the figure is not a gate, and it has not been re-measured at the deployed tree |
| Multi-turn context | Conversation scoped to authenticated user and active patient | Follow-up evals; context-isolation tests | Verified 2026-09-16 — conversation bound server-side to (site, user, pid); follow-ups keep the window and reference resolution (`ISO-FOLLOWUP-CHAIN-001`); a new conversation for the same chart starts empty (`ISO-NEW-CONVERSATION-001`); 30-minute idle close; transcript restore behind a fresh ticket. Added since: the same question in a fresh conversation is answered from the chart, not from memory (`ISO-FRESH-REPEAT-001`, 2026-09-17), and a recent conversation resumes only for its bound open patient (`ISO-RECENT-PATIENT-RESUME-001`, 2026-09-18) — the case that caught the split-clock regression on 2026-09-20 (evals row below) |
| Tool invocation and chaining | Strict read-only tools for encounters, meds, problems, allergies, labs, and notes | Traces and tool integration tests | Verified 2026-09-16 — UC-01 chains encounters into the windowed fan-out (7 tools); follow-ups plan strict-schema tool calls (`ISO-FOLLOWUP-CHAIN-001`: three turns, notes tool called on the third; `CONF-NOTE-VS-LIST-N-001` cites `form_clinical_notes`). Every tool call audited (`copilot-tool-read`, 7 rows per UC-01 turn) and visible as a node span in Langfuse. Since the batched gateway (`b40d456`; ADR-0003 status note 2026-09-19) a retrieval round travels as one batched gateway request, with the token, per-tool ACL and audit-before-data checks still run once per requested tool; since 2026-09-19 five questions whose wording is the agent's own (two starter chips and three suggestion chips) skip the plan model call through the `KNOWN_PLANS` table (`agent/app/graph/nodes.py`), while model-written chips and any other typed question still go through `plan` |
| Source attribution for every claim | Claim/source structured output and clickable record citations | Citation correctness and unsupported-claim evals | Verified 2026-09-16 — every non-absence claim carries source ids that resolve to `sources[]` with chart links (`CIT-UC01-A2-001`, `REG-HEAVY-001`, `every_claim_cited` on 20 live turns); citations open the record in OpenEMR from the panel |
| Domain constraint enforcement | Deterministic rule/policy checks after generation | Verifier unit tests and decision metrics | Verified 2026-09-16 — lexicon and typed-fact rules: advice wording withheld (`CIT-ALTERED-FACTS-001`), no cross-unit lab comparison (`LAB-UNIT-MISMATCH-K-001`), corrected result stated as corrected (`LAB-CORRECTED-M-001`), no invented indication (`MISS-INDICATION-F-001`), absence states distinct (`MISS-ALLERGY-REVIEWED-H-001`, `MISS-ALLERGY-UNDOC-I-001`), injected note instructions quoted not obeyed (`INJ-NOTE-O-001`); paraphrased advice and inference ("it would be wise to", "points toward") rejected like the direct wording after the lexicon was widened on 2026-09-16 (`CIT-PARAPHRASE-ADVICE-001`, `agent/app/verifier.py` FORBIDDEN, and the runner's own `ADVICE_RE` invariant) |
| Verification before response delivery | Model output quarantined until verifier passes | Architecture sequence; fail-closed tests | Verified 2026-09-16 — verifier between narrate and render with one repair; altered value, fabricated source, and advice wording withheld (`CIT-ALTERED-FACTS-001`); summary paragraph admitted only when nothing is withheld and every number is grounded (`CIT-SUMMARY-GATE-001`, ADR-0006 §7; since 2026-09-19 dates are grounded as dates, numbers in canonical form, and a replaced summary restates the first verified claims instead of counting them — `CIT-SUMMARY-GROUNDING-001`, decision 7 amendment); live happy path every claim cited and resolvable (`CIT-UC01-A2-001`) |
| Authorization and access control | OpenEMR session/ACL check on every tool request; patient-scoped delegation | Role and cross-patient adversarial tests; audit events | Verified 2026-09-16 — parity gateway (ADR-0002) with recorded evals: Front Office gets no clinical section (`AUTH-FRONTDESK-001`, regression for the aclCheckIssue fail-open), patient switch closes the conversation (`AUTH-SWITCH-001`), squad chart denied at start (`AUTH-SQUAD-001`), forged pid rejected by schema, tampered and expired tickets refused, parity cases `AF-ACL-OTHER` and `AF-ACL-UNSCHED` allowed and audited. Break-glass verified by hand only (needs a group change) |
| HIPAA-aware PHI handling | Minimum necessary payloads, encryption, secret management, redacted operational logs | Threat model and audit evidence | In progress — compliance audit complete (`docs/audit/compliance.md`: PHI data-flow inventory, BAA implications, retention, breach notification, proposed audit events). **Implemented:** projected fields and row caps per tool with `truncated` (`Gateway/Tools/AbstractTool.php`); age-band-and-sex patient context, never name or DOB (`Gateway/Tools/PatientContextTool.php:46-57`, `agent/app/contracts/tools.py:94`); PHI-free telemetry by default via a payload mask on every Langfuse span (`mask` in `agent/app/telemetry.py`, installed unless `trace_content` is on; in masked mode exception text is reduced to the class name since 2026-09-19). The demo Droplet runs with content capture on (`COPILOT_TRACE_CONTENT` defaults to 1 in its compose file; ADR-0007 amendment 2026-09-19), so there prompts, the evidence pack and the rendered answer do reach the tracer, which the ADR permits only on the stated assumption that the tracer is inside the compliance boundary and because the deployment holds synthetic patients only; PHI-free structured JSON logs that never carry bodies, prompts, or records (`agent/app/logging_setup.py`); an OpenEMR audit row before every tool read and on every gateway denial (`Gateway/Audit.php`, `public/gateway/tools.php`); since 2026-09-19 a `copilot-model-disclosure` row (user, patient, provider, model id, tool names, record count, ids; never content) written before a retrieval batch's records are returned for a model-backed turn, the batch answering `audit_unavailable` if the row cannot be written — read back from the deployment in `docs/audit/evidence/compliance/04-model-disclosure-rows-2026-09-19.md` (one row per batch, two on a UC-01 first turn; the fail-closed branch has not been exercised there); credentials as Docker file secrets, not environment variables (`infra/digitalocean/runtime/compose.yaml`). **Not implemented:** `copilot-llm-call` and `copilot-verification-result` audit events (neither string appears in the code; token counts, duration and verifier outcomes stay on the Langfuse trace and `/metrics`), the 24 h retention sweep (`ConversationRepository::sweep()` has no caller), agent egress restriction, KMS-managed keys, and any BAA. No HIPAA compliance is claimed |
| Transparent failure behavior | Partial response, retry limits, deterministic fallback, visible errors | Dependency-failure evals and demo | Verified 2026-09-16 — model outage and budget halt render the deterministic cited brief with a `narrative_unavailable` or `model_budget_exhausted` limitation (`MODEL-OUTAGE-001`, `MODEL-BUDGET-001`, about 1 s); lab tool outage marks the section unavailable and never claims labs absent (`TOOL-OUTAGE-LABS-001`); panel names timeouts and closed conversations plainly. Offline since 2026-09-17: the third turn inside a minute at `turns_per_minute = 2` is 429 `rate_limited` and counted in `copilot_denials_total` (`agent/tests/test_api.py::test_third_turn_in_a_minute_is_429_rate_limited`); the model circuit breaker opens after three provider failures and closes after the cooldown (`agent/tests/test_controls.py`, two tests); a graph exception is a generic 500 `internal_error` on the JSON path and an `error` event on the SSE path with `copilot_turns_in_flight` back at 0 (`test_turn_failure_is_generic_500_and_leaves_no_turn_in_flight`). Found and fixed the same day: a real provider failure inside a generation span was converted to `RuntimeError` by the telemetry context managers and would have bypassed the fallback (`test_exception_inside_an_observation_propagates_unchanged`) |
| Correlation ID across boundaries | One ID propagated through UI, gateway, agent, tools, LLM, verifier, and logs | Searchable end-to-end trace | Verified 2026-09-16 — one id (`<conversation>.<turn>`) from the ticket through the response header and body (`OBS-CORRELATION-001`), 8 agent log lines by node, 8 audit rows, and the Langfuse trace with 19 nested observations; walkthrough with a real turn in `docs/operations/correlation-id-walkthrough.md`. The three agent log lines that lacked the id (generation-span and tool-observation failures in `telemetry.py`, `claims dropped by contract` in `model.py`) carry it since 2026-09-17, passed explicitly from the graph nodes through the model port (`agent/tests/test_telemetry.py::test_observation_failure_logs_the_correlation_id`, `::test_dropped_claims_log_carries_the_correlation_id`); the module's PHP `error_log` calls (three then, six at 2026-09-20 after the batched gateway, the disclosure row and `BriefPolicy` each added one) carry no id and remain Week 2. The `copilot-model-disclosure` audit row carries the same `correlation_id` since 2026-09-19 |
| Canonical strict contracts | Pydantic/JSON Schema source of truth for requests and tool I/O | Generated schemas and contract tests | Verified 2026-09-15 — `agent/app/contracts` exports nine schemas to `contracts/schema/` with a drift check; recorded gateway responses validate; a patient id in any params is a schema error |
| Week 2 lab PDF upload and extraction preview | #34 stores one authorized synthetic `lab_pdf`; #35 reads it through the delegated source seam, validates every lab field state, resolves citations, and renders a review-only preview | `docs/week2/slice1a-upload-contract.md`; `docs/week2/slice1b-extraction-preview.md`; `agent/tests/test_document_contracts.py`; `agent/tests/test_intake_extractor.py`; `agent/tests/test_api.py` | In progress 2026-09-22 — strict schemas (13 exported), deterministic complete/partial/prompt-injection/tamper/outage/denial tests, changed-endpoint PHP lint, and 11 module PHPUnit tests are green. An authenticated local synthetic smoke passed upload → fresh ticket → reauthorized source read → complete six-field worker preview. The local stack has no public agent edge route, so browser-through-edge smoke, deployment, release telemetry, starter gate, and video remain outstanding; proposed facts are not persisted or eligible for chat claims. |
| Request/tool/LLM observability | Structured events and distributed traces | Trace linked from demo | Verified 2026-09-16 — Langfuse trace per turn: node spans, `plan`/`narrate`/`repair` generations with token usage and cost, tool status and rejections in span metadata, PHI masked by default (ADR-0007; the demo Droplet runs content capture on per the 2026-09-19 amendment, synthetic patients only, and a turn that shows a summary now also carries the `summary_model_kept` score, each generation a `prompt_version` hash). Eval run 2026-09-16: 28 live turns, p50 13.7 s, p95 26.8 s, 55 model calls |
| Real-time operational dashboard | Requests, errors, p50/p95, tool calls, retry count, verification outcomes, tokens, cost, and queue/in-flight depth | Dashboard URL/export and screenshot | Verified 2026-09-16 — Langfuse dashboard "Clinical Co-Pilot" with nine panels mapped to the PRD metrics (requests, p50/p95, errors, retries as repair generations, tool calls and failures per tool from TOOL observations, tokens, cost); `docs/operations/langfuse-dashboard.md`. Sanitized evidence for evaluators without project access: panel renders and one trace export with its correlation id in `docs/audit/evidence/observability/` (2026-09-16). `/metrics` carries the same counters for the alert job. Added 2026-09-17: `copilot_verification_total{outcome}`, `copilot_turns_in_flight` (queue depth; `copilot_in_flight` counts every HTTP request), and the bounded `reason` label on `copilot_tool_calls_total`; every trace carries the `verification_passed` and `turn_error` scores. Added 2026-09-19: the usage-funnel counters `copilot_panel_events_total{event}` (`chart_open`, `brief_started`, `drawer_open`) and `copilot_conversation_first_turn_total{turn_type}` on `/metrics` (`docs/operations/usage-funnel.md`; in-process, reset on every deploy, no real-session figures recorded yet). The verification pass/fail and error-rate panels over those scores were built in the Langfuse UI on 2026-09-18 and seen drawing data on 2026-09-20; the committed panel renders are the 2026-09-16 nine-panel set and predate both |
| Alert definitions and response | Latency, error-rate, and tool-failure alerts | Alert rules and runbook procedures | Verified 2026-09-16 — three rules (turn latency, error rate, tool failure) evaluated from `/metrics` by `agent/app/alerts.py` with thresholds from `KEY_METRICS.md`; 28 tests; runbook with first response, mitigation, and resolution in `docs/operations/alerts.md`; scheduled by the `alerts` service in `infra/digitalocean/runtime/compose.yaml` every 300 s (added 2026-09-17, on the host from the M3 deploy; before that nothing scheduled the rules); webhook wiring documented. Since 2026-09-17 `copilot_tool_calls_total` carries a bounded `reason` label and `forbidden` is excluded from the failure numerator (kept in the denominator), so six front-desk denials among forty calls no longer warn (`test_tool_failure_ignores_forbidden_denials`). Delivery proven end to end 2026-09-20: the service reads a Slack webhook from a Docker file secret (`--webhook-file /run/secrets/slack_alert_webhook`; never argv, never in the repository) and posts a one-line `text` summary plus the alert record. Five faulted turns (`X-Copilot-Fault: tool:medications`) produced two `page` alerts with exact denominators — 5 of 5 `medications` calls, and 5 of 35 tool calls (14.29%) against the 5% threshold — and both reached the channel (`webhook_delivered` true). Proving it found and fixed two defects: `copilot_tool_calls_total` had recorded no failure for tools the agent answers without a gateway call since the batched gateway, so the numerator was structurally zero (`dbf5372`), and Slack rejected a body without `text` (`0471178`). Evidence `docs/audit/evidence/observability/alerts-slack-2026-09-20.log`, which also records that nothing fired during the 761 s live-suite CI job 79057 (pipeline 24351). `agent/tests/test_alerts.py` holds 32 tests at 2026-09-20 |
| Boundary/invariant/regression evals | Versioned clinical fixtures and machine-readable eval cases | `evals/`; CI result artifact | Verified 2026-09-20 — 48 cases in `evals/cases/`, covering every cohort defect patient except `AF-DQ-Q` (the vitals `0` sentinel; there is no vitals tool in Week 1) plus the authorization, isolation and failure-mode cases, in three tiers: a 15-case golden set behind a blocking "Golden set integrity" gate, a 4-case holdout tier, and behavioural coverage by category (`evals/README.md`). Runner `evals/run.py` asserts invariants on every turn, prints the KEY_METRICS release-gate table with five states (PASS, FAIL, NOT RUN which blocks, NOT MEASURED, NOT CONFIGURED) judged against the full case manifest, and writes a scorecard over model-backed turns; `--repeat` for flakiness and `evals/compare.py` for A/B. **Release run `evals/results/2026-09-20T051146Z-0f11642.md`**, taken while `c37b9e6` was deployed (the runtime tree of `0f11642` is byte-identical to it): 48 cases x 3 attempts, 123 of 124 attempts passed, every blocking gate PASS, golden 29/29 attempts (15 cases, the 7 live ones run three times), citations 615/615 resolved, p95 15.8 s, $0.0104 per model-backed turn, no 5xx; scorecard 12.7% repair rate, 1.1% withheld, 4.25 claims and 1.56 model calls per turn. The deployed tree then moved to `478f432` for the alert fixes (four runtime commits, one of them on the turn path, `dbf5372`), and **`evals/results/2026-09-20T064022Z-4d2a9fd.md`** re-ran all 48 cases once against it: 48 passed, every blocking gate PASS, golden 15/15, holdout 4/4, citations 206/206, p95 20.0 s, $0.0113 per model-backed turn, no 5xx; the tag `week1-final` differs from `478f432` only in docs and eval results. Against the early-submission baseline (`…-0f11642-vs-a4a5856.md`): p95 24.1 s to 15.8 s, task success 95% to 100%, cost $0.0127 to $0.0104. The release run's one miss, `CONF-DUP-NAMES-C2-001`, is a holdout hedging flip that passed 2 of 3; flakiness stays confined to model wording while every deterministic assertion has passed every run. What the suite caught that a demo would not: at `6c787bd` it failed `ISO-RECENT-PATIENT-RESUME-001` three times of three, which traced to a half-applied timezone putting two clocks in one datetime column and silently breaking conversation resume (`docs/audit/evidence/performance/brief-on-open-2026-09-20.md` §10). Offline subset in CI (`test:evals-offline`); the full live suite is the manual `test:evals-live` job, last green as job 79057 (761 s) in pipeline 24351 on `4985e52d`, ref `week1-final`, whose report is a CI artifact and is not committed under `evals/results/`. |
| Runnable API collection | Core workflows, health/readiness, and failure examples | Bruno/Postman collection run in CI | Verified 2026-09-16 — `docs/api-collection/` (Bruno): 21 requests, 14 tests, 41 assertions passing against the deployment as `audit-physician` after the summary and suggestions change; asserts summary, suggestions, and contract 1.2.0 (`problem_status` claim type added 2026-09-16). CI runs the offline eval subset; the live collection needs the deployment and the demo password, so it stays a pre-deploy step. A 22nd request, `1 Session/06 Brief on chart open.bru`, was added 2026-09-19 with module 0.5.0 (asserts the `brief_on_open` boolean and `module_version`); no recorded collection run includes it |
| Separate health and readiness | Liveness plus meaningful OpenEMR, LLM, and observability dependency checks | Endpoint contract/integration tests | Verified 2026-09-20 — agent `/health` and `/ready` live with per-dependency detail and 503 when any fails: gateway ping (`ping.php`), `models.retrieve` on the configured model when the key exists, delegation secret readable, state store writable. The observability check probes the tracer since 2026-09-17: `check_tracer` (`agent/app/readiness.py`) sends `GET /api/public/projects` to the Langfuse host with basic auth and a 5 s timeout, and an unreachable or non-200 tracer fails readiness (`agent/tests/test_health.py::test_ready_is_503_when_tracer_unreachable`); the three network checks run concurrently. Verified on the deployment 2026-09-20: `/copilot-api/ready` returned `status: ready` with `{"name": "tracer", "ok": true, "detail": "reachable"}` alongside gateway, LLM provider, delegation secret and state store. OpenEMR's own `readyz` is deliberately not proxied and is unrouted at Caddy (SEC-MED-007). The panel probes `/health`, not `/ready` (`public/assets/js/copilot.js:717`; since 2026-09-19 the same probe carries `?panel=` for the usage-funnel counters). Collection asserts the 503-iff-failure rule |
| Baseline infrastructure profile | CPU, memory, latency, throughput under named scenario | Versioned load-test report | Verified 2026-09-18 — `docs/audit/evidence/performance/baseline-2026-09-18.md`, sampled over SSH every 5 s by `docs/audit/scripts/droplet-stats.sh` during each load level, CSVs in `evals/load/results/`. Idle: `openemr` CPU 0.6%/6.3% mean/peak, `database` 0.0%/0.1%, 11 httpd procs, 1,195 MiB host memory used. Real model at 10 users: `openemr` 20.7%/51.9%, `database` 33.4%/91.0%, `agent` 5.7%/19.8%, 43 MariaDB connections, `load1` 1.35. Real model at 50 users: `openemr` 54.7%/**103.2%**, `database` 59.0%/**111.0%**, `agent` 11.5%/43.7%, 58 httpd procs, 150 connections, `load1` 24.47. Throughput 13.5 turns/min at 10 users and 32.2 at 50. Two findings: CPU saturates well before 50 users and OpenEMR's Apache/PHP plus MariaDB — not the agent — is the ceiling; memory never was a constraint (host available never below 2,389 MiB), so ADR-0001's 8 GiB fallback trigger did not fire. Single-request local and public baselines remain in `docs/audit/performance.md` |
| Load tests at 10 and 50 concurrent users | Realistic authenticated conversation scenario | p50/p95/p99 and error-rate results | Verified 2026-09-18 — `evals/load/run_load.py` against the live Droplet: 10 users real-model p95 45.0 s (2 errors, 10%), 50 users 31/50 VUs completed with 90% of tool-gateway calls unavailable, both contradicting the 30 s `KEY_METRICS.md` threshold. A `--fault model` control run (zero model calls) reproduced the same magnitude at 50 users, isolating the cause to OpenEMR's Apache/PHP and MariaDB CPU, not the agent (container CPU data: `openemr`/`database` each saturate a full vCPU core; `agent` never exceeds 54% peak). `docs/audit/evidence/performance/load-test-2026-09-18.md` and `baseline-2026-09-18.md`. Measured before the pooled gateway client (`ba3105b`), the batched gateway (`b40d456`), low-effort follow-ups and brief on chart open: the after-fix run on the same Droplet took real-model turn p95 at 50 users from 43.8 s to 30.5 s without moving the ceiling (`agent-perf-fix-2026-09-18.md`), and the batched gateway was measured on rehearsal Droplets (`batched-gateway-2026-09-19.md`); the 10- and 50-user levels have not been re-run at the deployed tree |
| AI cost analysis | Actual development spend plus 100/1K/10K/100K-user projections | `AI_COST_ANALYSIS.md`; measured token mix | Verified (document) 2026-09-20 — `AI_COST_ANALYSIS.md`, first cut 2026-09-16: measured token mix per turn from `/metrics` (about 1.0K uncached input, 3.6K cache read, 1.95K output, 2.75 model calls, $0.0223 per turn at published Sonnet 5 prices; since 2026-09-17 the projection the eval cost gate judges against), development spend (Part A, through 2026-09-20), the daily budget from the token halt, 100/1K/10K/100K projections with labeled assumptions, sensitivity, and the cheapest levers. The load-test baseline is folded in as "What breaks first, by tier" (2026-09-18 data). The eval scorecard prices a model-backed turn at $0.0104 in the release run and $0.0113 in the run at the deployed tree (evals row above) |
| Repository setup and architecture guide | Evaluator-oriented README additions | Clean-checkout rehearsal | Verified 2026-09-16 — `README_AGENT_FORGE.md` (evaluator README: live URL, logins by path, recommended patients, architecture overview with ADR list, repository map, test and collection commands, observability, limitations, status); `docs/deployment/digitalocean.md` was unchanged at that date, while `SETUP.md` was updated in that milestone to describe the deployment as it stood; the runbook has since gained the rehearsal timings (2026-09-18) and a "Current Deployment (2026-09-20)" section. GitLab is the submission URL; OpenEMR's `README.md` opens with a challenge header (49 lines on 2026-09-17, longer since: what is live, the deliverables table, pointers to `README_AGENT_FORGE.md`, the live URL and `SETUP.md`), with the upstream README kept below it from its own `# OpenEMR` heading |
| Early and final 3–5 minute demos | Scripted physician workflow plus engineering evidence | Video links and scripts | Verified 2026-09-20 — early demo recorded and uploaded 2026-09-16 (<https://youtu.be/oxm9xqJpiY8>); final demo recorded and uploaded 2026-09-20 (<https://youtu.be/ZBzNHUv8fps>; script `docs/DEMO_SCRIPT.md`) |
| Technical and AI interviews | Evidence-backed architecture and failure-mode explanations | Interview preparation notes | In progress — `docs/INTERVIEW_NOTES.md` answers the PRD pre-search checklist with evidence pointers (2026-09-16). The technical interview was held 2026-09-17; its three questions, each closed with a disposition and evidence, are recorded in `docs/INTERVIEW_FEEDBACK.md` (filled 2026-09-20). The final AI interview has not been held |
| Final social post | Concise product story and demo media | Post link in submission checklist | Verified 2026-09-20 — published: <https://x.com/_hdx_/status/2101640176279961620>. Draft `docs/SOCIAL_POST.md` (LinkedIn and X versions, numbers from the release run) |

## Week 2 planned traceability

Every row below is an approved design and remains `Not started` until the
linked implementation evidence exists. The integrated order and checkpoint
criteria are normative in
`docs/specs/week2-integrated-implementation-plan.md`; ADR-0008 through
ADR-0015 hold the underlying decisions.

| Week 2 requirement | Planned implementation or artifact | Required verification evidence | Status |
| --- | --- | --- | --- |
| Two document types and patient association | Patient-bound explicit upload for `lab_pdf` and `intake_form`; OpenEMR owns immutable source bytes and the module owns idempotent source mappings | Positive upload for both types; same-intent retry; duplicate warning; active-patient, ACL, squad, break-glass, type/size/page, source-hash, and decrypted-log-path negatives | In progress — #34/#35 implement the `lab_pdf` path through session/CSRF-bound upload, native OpenEMR storage, a fresh delegated source read, strict six-field extraction, resolver/verifier, and review-only preview. #37 adds the closed intake discriminator, synthetic PDF fixture, conditional intake limits, and the same intent/source mapping (`docs/week2/slice2a-intake-contract.md`). #38 extends the same worker and UI path to require the persisted intake discriminator and produce a review-only cited intake preview (`docs/week2/slice2b-intake-extraction-preview.md`). #39 adds the automatic 17-case shared gate and bounded type-labeled preview telemetry; its deployed synthetic smoke is recorded in `docs/week2/slice2c-release-gate.md`. |
| Strict document extraction schemas | Bounded intake-extractor worker with deterministic rendering/OCR, at most one region retry, immutable extraction versions, proposed facts, and page/box/quote evidence | Cross-runtime schema snapshots; multi-page lab and complete intake round trips; malformed, rotated, unreadable, ambiguous, conflicting, missing, injected-text, timeout, cancellation, and no-same-run-answer cases | In progress — #34/#35 export the strict `lab_pdf` proposed-extraction/citation contract, fixture, deterministic text-layer extractor, per-field partial states, resolver/verifier, and review-only UI. #37 exports a strict review-only intake proposal schema with demographics, chief concern, medications, allergies, family history, distinct field states/confidence, and citations. #38 implements the bounded synthetic intake resolver/verifier and cited preview: ambiguous, conflicting, and missing fields yield `partial` while independently verified fields render; altered citation identity/field/quote data is withheld. Local unit/schema evidence is recorded in `docs/week2/slice2b-intake-extraction-preview.md`; deployment and broader imperfect-scan coverage remain incomplete. |
| Derived facts stored with OpenEMR integrity | Physician review plus one UI-only, authorized, audited, idempotent transaction creating immutable module-owned Observation-shaped lab or QuestionnaireResponse-shaped intake records | Approve/correct/reject, partial review, stale review, patient switch, missing write ACL, replay before/after commit, injected transaction failure, amendment/withdrawal, exact authorized read-back, and one-target proof | Not started — ADR-0008/0011; no native-list writes |
| Basic hybrid RAG plus rerank | Approved eight-topic versioned USPSTF corpus; heading-aware chunks; FTS5 + pinned BGE-small exact FAISS, RRF, and pinned local MiniLM ONNX rerank-to-five | Committed 160-chunk/32-query quality/resource benchmark; keyword-only, semantic-only, rerank, filter, no-result, stale/wrong corpus, hash, outage, timeout, and injection cases | Not started — ADR-0010 |
| Supervisor plus intake-extractor and evidence-retriever workers | Deterministic typed-event route table, explicit PHI-free handoffs, separate extraction jobs, bounded parallel patient/guideline read branches, and join before verification | Route/transition matrix; malformed/stale/duplicate handoffs; injection, deadline, retry, cancellation race, one-terminal-status, follow-up freshness, and lane-failure tests; reconstructable sanitized trace | Not started — ADR-0013; CAP-12 |
| Machine-readable citation contract and PDF bounding-box overlay | Closed source/citation/claim unions for native OpenEMR records, reviewed document fields, and guideline chunks; in-place source view with reauthorization and integrity checks | Unknown/wrong/stale source rejection; exact value/quote and hash checks; page/box focus; printed versus reviewed correction; keyboard, small-screen, and source-unavailable UI evidence | Not started — ADR-0012 and source-review interaction contract |
| Safe final response and critic behavior | Existing deterministic verifier extended for the new source/claim unions; patient and guideline lanes remain separate; no same-run proposals, applicability conclusion, or cross-lane clinical synthesis | Patient-only, guideline-only, mixed, altered fact, removed citation, stale source, unsafe action wording, proposed-as-final, exact excerpt, and one-lane failure cases | Not started — deterministic verifier is mandatory core; separate model critic remains extension work |
| At least 50 golden Boolean-rubric cases | Retain all 48 Week 1 cases and add at least 35 new deterministic-verdict golden cases, yielding at least 50 golden and 83 total release-corpus cases | Manifest and allocation report; `schema_valid`, `citation_present`, `factually_consistent`, `safe_refusal`, and `no_phi_in_logs`; at least half new cases negative/adversarial; holdout discipline | Not started — ADR-0015 |
| Regression-blocking Git hook or equivalent | Versioned local pre-push feedback plus automatic, required, non-`allow_failure` GitLab contract and candidate-release jobs using an approved immutable baseline | Threshold/comparison tests; branch protection; candidate identity and hashes; full corpus artifact; temporary grader-style mutation produces red and removal produces green against the same baseline | Not started — current live job is manual and does not satisfy this row |
| PHI-free observability and cost tracking | Route, handoff, tool, retrieval, extraction, verifier, eval, latency, token, cost, confidence-bucket, and outcome telemetry with one correlation chain; raw content remains in protected stores | Canary scans across logs/traces/metrics/handoffs/checkpoints; correlation reconstruction; dashboard/export; all token billing classes and per-capability spend; tracer outage does not block core readiness | In progress — #39 adds a bounded `document_type` to preview trace metadata and Prometheus labels, masks all other document metadata by default, and gates both types with telemetry canaries (`docs/week2/slice2c-release-gate.md`). Deployed-channel canary scans, correlation reconstruction, retrieval, and full cost evidence remain unmeasured. |
| Deployed, measurable Week 2 flow | One DigitalOcean `c-4` with isolated OpenEMR, MariaDB, chat/supervisor, extraction, retrieval, alerts, and Caddy services; capability readiness, retention, storage, concurrency, and durable daily spend controls | Clean deploy/rollback; readiness fault matrix; one-hour and 24-hour sweepers; three-version/storage refusal; cold/warm and mixed-load p50/p95, CPU/RSS/disk/error/cost report; release configuration proof | Not started — ADR-0014; one-host failure domain accepted |
| Week 2 architecture and evaluator handoff | `W2_ARCHITECTURE.md` synthesized from accepted decisions, updated `USERS.md`, `KEY_METRICS.md`, setup/runbooks, traceability, demo script/video, and evidence index | Architecture-document checklist from the integrated plan; every capability maps to UC-01..03/05/06 and a PRD row; observed versus planned statements remain explicit | Not started — the decision map and authoring contract are complete |

The critic agent, third document type, trend widget, contextual-retrieval
enhancements, schedule sweep, native clinical-list writes, and non-synthetic
deployment are extensions or separate destinations. They do not satisfy or
block the mandatory rows above.

## Known PRD Naming Ambiguity

The hard-gate section names `USERS.md`, while the submission table names
`USER.md`. `USERS.md` is the canonical document in this repository, and
`USER.md` points evaluators and automated checks to it.
