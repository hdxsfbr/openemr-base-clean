# Requirements Traceability

This matrix maps the AgentForge PRD to planned implementation and evaluator
evidence. Update the status and evidence links as work lands. A requirement is
not complete merely because code exists; its proof must also be accessible.

Status values: `Not started`, `In progress`, `Verified`, or `Blocked`.

| Requirement | Planned implementation or artifact | Verification evidence | Status |
| --- | --- | --- | --- |
| OpenEMR runs locally with realistic sample data | Development compose stack plus documented clinical demo cohort | `SETUP.md`; repeatable seed command; screenshots or smoke test | Verified locally 2026-09-14 — bundled demo dataset plus deterministic synthetic cohort `af-cohort-v1` (`evals/fixtures/cohort/`: 26 patients, 50 encounters, 134 lab results, 67 notes; identical checksums across reloads; post-load checks pass). Loaded on the deployment by the one-shot `demo-seed` job since 2026-09-15 (26 patients, post-load checks passing; `docs/deployment/digitalocean.md` "Manual Cycle") |
| Publicly accessible deployment | Single-Droplet Terraform, restricted Compose networks, and Caddy TLS ingress | `docs/deployment/digitalocean.md`; external smoke test | Verified 2026-09-15 — `v0.1.0-skeleton` live at `https://openemr-137-184-4-22.sslip.io`: project image with the module, agent service, deny-by-default edge (probe: 20 sensitive paths 404), demo users and 26-patient cohort seeded, panel rendering on cohort charts. Kept up during build days; `v0.2.0-slice` and later builds were deployed to the same hostname with `deploy.sh`, and every eval report in `evals/results/` (2026-09-16) and the manual CI job `test:evals-live` target it. Owned hostname still pending |
| Backup, restore, rollback and clean deploy | `infra/digitalocean/backup.sh` (encrypted archive: database dump, `openemr_sites` and `agent_state` volumes, secrets and `.env`, under `~/.config/agentforge/backups/`), `restore.sh` (secrets first, database volume re-initialised from them, dump loaded, volumes replaced), rollback by redeploying tag `week1` from a worktree, rehearsal runbook with a timing table in `docs/deployment/digitalocean.md` | Rehearsal timings and the golden run on the rehearsal host (M4) | In progress 2026-09-17 — scripts and runbook written; nothing has run against a host; the default workspace and the live Droplet are excluded from the rehearsal by construction |
| Full audit with approximately 500-word summary | Security, performance, architecture, data-quality, and compliance passes | `AUDIT.md` (500-word summary; one section per PRD area with every finding; remediation; plan changes; residual risk); detail and evidence in `docs/audit/` | Complete, owner-reviewed 2026-09-14 (Stage 3 gate passed) — all five areas audited on 2026-09-14 with live ACL tests, lead spot-checks, an 8-minute public cloud-window probe, and synthetic-cohort measurements. Authenticated Droplet latency and load tests remain later deliverables |
| Narrow target user and concrete workflow | Primary-care physician pre-visit workflow | `USERS.md`; user/use-case review | Verified (document) 2026-09-15, owner-approved — 90-second workflow moment (T−30 s to T+90 s), refusals, the per-chart-versus-schedule-sweep decision (sweep deferred as UC-04), rejected and scheduled use cases, and the CAP-01..08 capability table `ARCHITECTURE.md` traces to. Clinician-proxy validation (six checkboxes) still open and tracked in `USERS.md` |
| Every use case explains why an agent is appropriate | Explicit rationale and rejected UI alternatives per use case | `USERS.md` use-case sections | Verified 2026-09-15 — each of UC-01..03 carries a widget / sorted list / chart view / agent comparison, an "honest limit" stating where a non-agent would suffice, and a worked synthetic example; owner-approved. The clinician interview may revise wording |
| Architecture plan with approximately 500-word summary | Audit-informed context, components, trust boundaries, and tradeoffs | `ARCHITECTURE.md`; ADRs | Verified 2026-09-15 (Stage 5 gate passed) — revised against `AUDIT.md` §8 and ADR-0002/0003: 594-word summary, request path, per-turn delegation, CAP-01..08 traceability, tool matrix, contracts, LangGraph turn graph, checkpointed state, verifier, failure matrix, observability, API and Bruno collection design, deployment, Weeks 2–3 compounding. ADR-0004..0007 accepted 2026-09-15 |
| Key metrics with rationale | Safety, utility, latency, reliability, and cost metrics | `KEY_METRICS.md`; dashboard/eval links | Verified (definitions) 2026-09-15 — metrics, gaming defenses, decision thresholds (release gates, the three PRD alerts with responses, risk-acceptance rules) owner-approved; latency and cost thresholds provisional until the load baseline (2026-09-19) and clinician interview; dashboard and eval evidence links land with the implementation |
| Embedded agentic chatbot | OpenEMR patient-dashboard module | Multi-turn live demo and UI tests | Verified 2026-09-16 — chat transcript panel on the patient dashboard: summary paragraph, claims table with chart links, follow-up chips from the turn's records, live progress over server-sent events, history restored behind a fresh ticket, 30-minute idle close. Full UC-01/02/03 turns with Sonnet 5 in 12 to 28 s on the deployment; eval run 2026-09-16 (`evals/results/`) exercises the same path headlessly |
| Multi-turn context | Conversation scoped to authenticated user and active patient | Follow-up evals; context-isolation tests | Verified 2026-09-16 — conversation bound server-side to (site, user, pid); follow-ups keep the window and reference resolution (`ISO-FOLLOWUP-CHAIN-001`); a new conversation for the same chart starts empty (`ISO-NEW-CONVERSATION-001`); 30-minute idle close; transcript restore behind a fresh ticket |
| Tool invocation and chaining | Strict read-only tools for encounters, meds, problems, allergies, labs, and notes | Traces and tool integration tests | Verified 2026-09-16 — UC-01 chains encounters into the windowed fan-out (7 tools); follow-ups plan strict-schema tool calls (`ISO-FOLLOWUP-CHAIN-001`: three turns, notes tool called on the third; `CONF-NOTE-VS-LIST-N-001` cites `form_clinical_notes`). Every tool call audited (`copilot-tool-read`, 7 rows per UC-01 turn) and visible as a node span in Langfuse |
| Source attribution for every claim | Claim/source structured output and clickable record citations | Citation correctness and unsupported-claim evals | Verified 2026-09-16 — every non-absence claim carries source ids that resolve to `sources[]` with chart links (`CIT-UC01-A2-001`, `REG-HEAVY-001`, `every_claim_cited` on 20 live turns); citations open the record in OpenEMR from the panel |
| Domain constraint enforcement | Deterministic rule/policy checks after generation | Verifier unit tests and decision metrics | Verified 2026-09-16 — lexicon and typed-fact rules: advice wording withheld (`CIT-ALTERED-FACTS-001`), no cross-unit lab comparison (`LAB-UNIT-MISMATCH-K-001`), corrected result stated as corrected (`LAB-CORRECTED-M-001`), no invented indication (`MISS-INDICATION-F-001`), absence states distinct (`MISS-ALLERGY-REVIEWED-H-001`, `MISS-ALLERGY-UNDOC-I-001`), injected note instructions quoted not obeyed (`INJ-NOTE-O-001`); paraphrased advice and inference ("it would be wise to", "points toward") rejected like the direct wording after the lexicon was widened on 2026-09-16 (`CIT-PARAPHRASE-ADVICE-001`, `agent/app/verifier.py` FORBIDDEN, and the runner's own `ADVICE_RE` invariant) |
| Verification before response delivery | Model output quarantined until verifier passes | Architecture sequence; fail-closed tests | Verified 2026-09-16 — verifier between narrate and render with one repair; altered value, fabricated source, and advice wording withheld (`CIT-ALTERED-FACTS-001`); summary paragraph admitted only when nothing is withheld and every number is grounded (`CIT-SUMMARY-GATE-001`, ADR-0006 §7); live happy path every claim cited and resolvable (`CIT-UC01-A2-001`) |
| Authorization and access control | OpenEMR session/ACL check on every tool request; patient-scoped delegation | Role and cross-patient adversarial tests; audit events | Verified 2026-09-16 — parity gateway (ADR-0002) with recorded evals: Front Office gets no clinical section (`AUTH-FRONTDESK-001`, regression for the aclCheckIssue fail-open), patient switch closes the conversation (`AUTH-SWITCH-001`), squad chart denied at start (`AUTH-SQUAD-001`), forged pid rejected by schema, tampered and expired tickets refused, parity cases `AF-ACL-OTHER` and `AF-ACL-UNSCHED` allowed and audited. Break-glass verified by hand only (needs a group change) |
| HIPAA-aware PHI handling | Minimum necessary payloads, encryption, secret management, redacted operational logs | Threat model and audit evidence | In progress — compliance audit complete (`docs/audit/compliance.md`: PHI data-flow inventory, BAA implications, retention, breach notification, proposed audit events). **Implemented:** projected fields and row caps per tool with `truncated` (`Gateway/Tools/AbstractTool.php`); age-band-and-sex patient context, never name or DOB (`Gateway/Tools/PatientContextTool.php:46-57`, `agent/app/contracts/tools.py:72`); PHI-free telemetry via a payload mask on every Langfuse span (`agent/app/telemetry.py:26,54`); PHI-free structured JSON logs that never carry bodies, prompts, or records (`agent/app/logging_setup.py`); an OpenEMR audit row before every tool read and on every gateway denial (`Gateway/Audit.php`, `public/gateway/tools.php`); credentials as Docker file secrets, not environment variables (`infra/digitalocean/runtime/compose.yaml`). **Not implemented:** `copilot-llm-call` and `copilot-verification-result` audit events (neither string appears in the tree), the 24 h retention sweep (`ConversationRepository::sweep()` has no caller), agent egress restriction, KMS-managed keys, and any BAA. No HIPAA compliance is claimed |
| Transparent failure behavior | Partial response, retry limits, deterministic fallback, visible errors | Dependency-failure evals and demo | Verified 2026-09-16 — model outage and budget halt render the deterministic cited brief with a `narrative_unavailable` or `model_budget_exhausted` limitation (`MODEL-OUTAGE-001`, `MODEL-BUDGET-001`, about 1 s); lab tool outage marks the section unavailable and never claims labs absent (`TOOL-OUTAGE-LABS-001`); panel names timeouts and closed conversations plainly. Offline since 2026-09-17: the third turn inside a minute at `turns_per_minute = 2` is 429 `rate_limited` and counted in `copilot_denials_total` (`agent/tests/test_api.py::test_third_turn_in_a_minute_is_429_rate_limited`); the model circuit breaker opens after three provider failures and closes after the cooldown (`agent/tests/test_controls.py`, two tests); a graph exception is a generic 500 `internal_error` on the JSON path and an `error` event on the SSE path with `copilot_turns_in_flight` back at 0 (`test_turn_failure_is_generic_500_and_leaves_no_turn_in_flight`). Found and fixed the same day: a real provider failure inside a generation span was converted to `RuntimeError` by the telemetry context managers and would have bypassed the fallback (`test_exception_inside_an_observation_propagates_unchanged`) |
| Correlation ID across boundaries | One ID propagated through UI, gateway, agent, tools, LLM, verifier, and logs | Searchable end-to-end trace | Verified 2026-09-16 — one id (`<conversation>.<turn>`) from the ticket through the response header and body (`OBS-CORRELATION-001`), 8 agent log lines by node, 8 audit rows, and the Langfuse trace with 19 nested observations; walkthrough with a real turn in `docs/operations/correlation-id-walkthrough.md`. The three agent log lines that lacked the id (generation-span and tool-observation failures in `telemetry.py`, `claims dropped by contract` in `model.py`) carry it since 2026-09-17, passed explicitly from the graph nodes through the model port (`agent/tests/test_telemetry.py::test_observation_failure_logs_the_correlation_id`, `::test_dropped_claims_log_carries_the_correlation_id`); the module's three PHP `error_log` calls remain Week 2 |
| Canonical strict contracts | Pydantic/JSON Schema source of truth for requests and tool I/O | Generated schemas and contract tests | Verified 2026-09-15 — `agent/app/contracts` exports nine schemas to `contracts/schema/` with a drift check; recorded gateway responses validate; a patient id in any params is a schema error |
| Request/tool/LLM observability | Structured events and distributed traces | Trace linked from demo | Verified 2026-09-16 — Langfuse trace per turn: node spans, `plan`/`narrate`/`repair` generations with token usage and cost, tool status and rejections in span metadata, PHI masked (ADR-0007). Eval run 2026-09-16: 28 live turns, p50 13.7 s, p95 26.8 s, 55 model calls |
| Real-time operational dashboard | Requests, errors, p50/p95, tool calls, retry count, verification outcomes, tokens, cost, and queue/in-flight depth | Dashboard URL/export and screenshot | Verified 2026-09-16 — Langfuse dashboard "Clinical Co-Pilot" with nine panels mapped to the PRD metrics (requests, p50/p95, errors, retries as repair generations, tool calls and failures per tool from TOOL observations, tokens, cost); `docs/operations/langfuse-dashboard.md`. Sanitized evidence for evaluators without project access: panel renders and one trace export with its correlation id in `docs/audit/evidence/observability/` (2026-09-16). `/metrics` carries the same counters for the alert job. Added 2026-09-17: `copilot_verification_total{outcome}`, `copilot_turns_in_flight` (queue depth; `copilot_in_flight` counts every HTTP request), and the bounded `reason` label on `copilot_tool_calls_total`; every trace carries the `verification_passed` and `turn_error` scores. The verification pass/fail and error-rate panels over those scores are still to be built in the Langfuse UI, so the dashboard minimum is not yet fully on the nine panels |
| Alert definitions and response | Latency, error-rate, and tool-failure alerts | Alert rules and runbook procedures | Verified 2026-09-16 — three rules (turn latency, error rate, tool failure) evaluated from `/metrics` by `agent/app/alerts.py` with thresholds from `KEY_METRICS.md`; 28 tests; runbook with first response, mitigation, and resolution in `docs/operations/alerts.md`; scheduled by the `alerts` service in `infra/digitalocean/runtime/compose.yaml` every 300 s (added 2026-09-17, on the host from the M3 deploy; before that nothing scheduled the rules); webhook wiring documented. Since 2026-09-17 `copilot_tool_calls_total` carries a bounded `reason` label and `forbidden` is excluded from the failure numerator (kept in the denominator), so six front-desk denials among forty calls no longer warn (`test_tool_failure_ignores_forbidden_denials`) |
| Boundary/invariant/regression evals | Versioned clinical fixtures and machine-readable eval cases | `evals/`; CI result artifact | Verified 2026-09-20 — 48 cases in `evals/cases/`, one per cohort defect, in three tiers: a 29-case golden set behind a blocking "Golden set integrity" gate, a 12-case holdout tier, and behavioural coverage by category (`evals/README.md`). Runner `evals/run.py` asserts invariants on every turn, prints the KEY_METRICS release-gate table with five states (PASS, FAIL, NOT RUN which blocks, NOT MEASURED, NOT CONFIGURED) judged against the full case manifest, and writes a scorecard over model-backed turns; `--repeat` for flakiness and `evals/compare.py` for A/B. **Release run `evals/results/2026-09-20T051146Z-0f11642.md`** at a commit whose runtime tree is byte-identical to the deployed `c37b9e6`: 48 cases x 3 attempts, 123 passed, every blocking gate PASS, golden 29/29, citations 615/615 resolved, p95 15.8 s, $0.0104 per model-backed turn, no 5xx; scorecard 12.7% repair rate, 1.1% withheld, 4.25 claims and 1.56 model calls per turn. Against the early-submission baseline (`…-0f11642-vs-a4a5856.md`): p95 24.1 s to 15.8 s, task success 95% to 100%, cost $0.0127 to $0.0104. The one miss, `CONF-DUP-NAMES-C2-001`, is a holdout hedging flip that passed 2 of 3; flakiness stays confined to model wording while every deterministic assertion has passed every run. What the suite caught that a demo would not: at `6c787bd` it failed `ISO-RECENT-PATIENT-RESUME-001` three times of three, which traced to a half-applied timezone putting two clocks in one datetime column and silently breaking conversation resume (`docs/audit/evidence/performance/brief-on-open-2026-09-20.md` §10). Offline subset in CI (`test:evals-offline`); the full live suite is the manual `test:evals-live` job. |
| Runnable API collection | Core workflows, health/readiness, and failure examples | Bruno/Postman collection run in CI | Verified 2026-09-16 — `docs/api-collection/` (Bruno): 21 requests, 14 tests, 41 assertions passing against the deployment as `audit-physician` after the summary and suggestions change; asserts summary, suggestions, and contract 1.2.0 (`problem_status` claim type added 2026-09-16). CI runs the offline eval subset; the live collection needs the deployment and the demo password, so it stays a pre-deploy step |
| Separate health and readiness | Liveness plus meaningful OpenEMR, LLM, and observability dependency checks | Endpoint contract/integration tests | Verified 2026-09-20 — agent `/health` and `/ready` live with per-dependency detail and 503 when any fails: gateway ping (`ping.php`), `models.retrieve` on the configured model when the key exists, delegation secret readable, state store writable. The observability check probes the tracer since 2026-09-17: `check_tracer` (`agent/app/readiness.py`) sends `GET /api/public/projects` to the Langfuse host with basic auth and a 5 s timeout, and an unreachable or non-200 tracer fails readiness (`agent/tests/test_health.py::test_ready_is_503_when_tracer_unreachable`); the three network checks run concurrently. Verified on the deployment 2026-09-20: `/copilot-api/ready` returned `status: ready` with `{"name": "tracer", "ok": true, "detail": "reachable"}` alongside gateway, LLM provider, delegation secret and state store. OpenEMR's own `readyz` is deliberately not proxied and is unrouted at Caddy (SEC-MED-007). The panel probes `/health`, not `/ready` (`public/assets/js/copilot.js:485`). Collection asserts the 503-iff-failure rule |
| Baseline infrastructure profile | CPU, memory, latency, throughput under named scenario | Versioned load-test report | Verified 2026-09-18 — `docs/audit/evidence/performance/baseline-2026-09-18.md`, sampled over SSH every 5 s by `docs/audit/scripts/droplet-stats.sh` during each load level, CSVs in `evals/load/results/`. Idle: `openemr` CPU 0.6%/6.3% mean/peak, `database` 0.0%/0.1%, 11 httpd procs, 1,195 MiB host memory used. Real model at 10 users: `openemr` 20.7%/51.9%, `database` 33.4%/91.0%, `agent` 5.7%/19.8%, 43 MariaDB connections, `load1` 1.35. Real model at 50 users: `openemr` 54.7%/**103.2%**, `database` 59.0%/**111.0%**, `agent` 11.5%/43.7%, 58 httpd procs, 150 connections, `load1` 24.47. Throughput 13.5 turns/min at 10 users and 32.2 at 50. Two findings: CPU saturates well before 50 users and OpenEMR's Apache/PHP plus MariaDB — not the agent — is the ceiling; memory never was a constraint (host available never below 2,389 MiB), so ADR-0001's 8 GiB fallback trigger did not fire. Single-request local and public baselines remain in `docs/audit/performance.md` |
| Load tests at 10 and 50 concurrent users | Realistic authenticated conversation scenario | p50/p95/p99 and error-rate results | Verified 2026-09-18 — `evals/load/run_load.py` against the live Droplet: 10 users real-model p95 45.0 s (2 errors, 10%), 50 users 31/50 VUs completed with 90% of tool-gateway calls unavailable, both contradicting the 30 s `KEY_METRICS.md` threshold. A `--fault model` control run (zero model calls) reproduced the same magnitude at 50 users, isolating the cause to OpenEMR's Apache/PHP and MariaDB CPU, not the agent (container CPU data: `openemr`/`database` each saturate a full vCPU core; `agent` never exceeds 54% peak). `docs/audit/evidence/performance/load-test-2026-09-18.md` and `baseline-2026-09-18.md` |
| AI cost analysis | Actual development spend plus 100/1K/10K/100K-user projections | `AI_COST_ANALYSIS.md`; measured token mix | In progress — `AI_COST_ANALYSIS.md` first cut 2026-09-16: measured token mix per turn from `/metrics` (about 1.0K uncached input, 3.6K cache read, 1.95K output, 2.75 model calls, $0.022 per turn at published Sonnet 5 prices), development spend, 100/1K/10K/100K projections with labeled assumptions, sensitivity, and the three cheapest levers. Load-test baseline still to fold in |
| Repository setup and architecture guide | Evaluator-oriented README additions | Clean-checkout rehearsal | Verified 2026-09-16 — `README_AGENT_FORGE.md` (evaluator README: live URL, logins by path, recommended patients, architecture overview with ADR list, repository map, test and collection commands, observability, limitations, status); `docs/deployment/digitalocean.md` unchanged so far (a runbook delta is still pending), while `SETUP.md` was updated in this milestone to describe the deployment as it stands. GitLab is the submission URL; OpenEMR's `README.md` now opens with a 49-line challenge header (deliverables table, pointers to `README_AGENT_FORGE.md`, the live URL and `SETUP.md`), with the upstream README kept below it from line 51 |
| Early and final 3–5 minute demos | Scripted physician workflow plus engineering evidence | Video links and scripts | In progress — early demo recorded and uploaded 2026-09-16 (<https://youtu.be/oxm9xqJpiY8>; script `docs/DEMO_SCRIPT.md`); final demo pending |
| Technical and AI interviews | Evidence-backed architecture and failure-mode explanations | Interview preparation notes | In progress — `docs/INTERVIEW_NOTES.md` answers the PRD pre-search checklist with evidence pointers (2026-09-16); interviews not yet held |
| Final social post | Concise product story and demo media | Post link in submission checklist | Not started |

## Known PRD Naming Ambiguity

The hard-gate section names `USERS.md`, while the submission table names
`USER.md`. `USERS.md` is the canonical document in this repository, and
`USER.md` points evaluators and automated checks to it.
