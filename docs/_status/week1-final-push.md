# Status: `week1-final-push` — MILESTONE M2 (code and harness ready, nothing deployed)

Plan: [docs/FINAL_PUSH_PLAN.md](../FINAL_PUSH_PLAN.md), MILESTONE M2. Orchestrated
with the `task-to-production` skill from Phase 2; Phases 0 and 1 are the approved
plan document (do not re-audit, do not re-design). Live task tools
(`TaskCreate`/`TaskUpdate`) are absent in this session, so this file is the
durable record and the `/workflows` tree is the live one.

**Commit policy.** The orchestrator is the only committer. One commit per
workstream after the implement/verify workflow returns (file sets are disjoint,
so each commit is `git add <owned paths>`), one commit for the doc-apply pass,
and this file committed alone at every round boundary. Implementation agents
never run `git commit`.

**Started:** 2026-09-17 (Thursday evening PT), branch at `14dac2f`, tree clean,
60 agent tests, 45 eval cases.

| Workstream | Owner | Files owned | Acceptance criteria | Status |
|---|---|---|---|---|
| WS-AGENT: A1 tracer probe, A2 `copilot_turns_in_flight`, A3 verification outcome metric + trace scores, A4 correlation id on 3 log calls, A5 `reason` label and `forbidden` excluded from tool-failure rate, A6 `requirements.lock`, A7 control tests | WS-AGENT | `agent/app/**`, `agent/tests/**`, `agent/Dockerfile`, `agent/pyproject.toml`, `agent/requirements.lock`, `agent/README.md`; plus `.gitlab-ci.yml` for A6 only (unlisted in the plan's ownership map; granted here because A6 names it) | `cd agent && .venv/bin/python -m pytest -q` (more than 60 tests, all pass); `.venv/bin/python -m app.contracts.export --check`; `agent/.venv/bin/python evals/run.py --offline-only` passes; fresh venv from `requirements.lock` passes the suite; `docs/_pending/WS-AGENT.md` carries deltas for ARCHITECTURE.md, KEY_METRICS.md, ADR-0004, ADR-0007, the traceability matrix, the checklist | verified (round 1: both lenses pass; six minors carried to R2-AGENT) |
| WS-EVAL: E1 cost gate at $0.0223, E2 `CONF-NOTE-VS-LIST-N-001` matcher decision (evidence-driven), E3 `ISO-FRESH-REPEAT-001` only | WS-EVAL | `evals/run.py`, `evals/compare.py`, `evals/README.md`, `evals/cases/**` | `evals/run.py --offline-only` and `--golden-only` parse and run (golden needs a deployment and is human-gated: parse + `--help` + a dry manifest load are the M2 check); gate table shows a real cost state; every new case has the required metadata and a `risk:` line; `manifest()` loads every YAML | verified (round 1 after one fix round; the `_cost_usd` under-count found by the completeness pass goes to R2-EVAL) |
| WS-LOAD: L1 `run_load.py`, L2 `droplet-stats.sh`, L3 expected-failure docstring | WS-LOAD | `evals/load/**`, `docs/audit/scripts/droplet-stats.sh` | `python -m py_compile evals/load/run_load.py`; `run_load.py --help` lists `--users`, `--label`, `--fault`; `bash -n docs/audit/scripts/droplet-stats.sh`; results JSON schema documented in the module docstring. The `--users 2` smoke is M3 and human-gated | verified (round 1 after one fix round; four doc-vs-code minors carried to R2-LOAD) |
| WS-INFRA: I1 `alerts` service, I2 `start.sh` recovery + `x-logging`, I3 `backup.sh`/`restore.sh`, I4 rehearsal runbook | WS-INFRA | `infra/**`, `docs/deployment/digitalocean.md` | `cd infra/digitalocean/runtime && docker compose config --quiet`; `bash -n start.sh && bash -n ../backup.sh && bash -n ../restore.sh`; runbook states in bold that the default workspace and 137.184.4.22 are never touched | verified (round 1; the plan's `docker compose config --quiet` line cannot pass verbatim, see contradiction 6; minors to R2-INFRA) |
| WS-CONTENT: X1 demo script with `<pending M4>` placeholders, X2 social post, X3 Week-2 handoff (one-page stub), X4 egress risk acceptance + rotation checklist as deltas | WS-CONTENT | `docs/DEMO_SCRIPT.md`, `docs/SOCIAL_POST.md`, `docs/WEEK2_HANDOFF.md`, `docs/INTERVIEW_NOTES.md`, `docs/INTERVIEW_FEEDBACK.md` | No number in DEMO_SCRIPT.md that does not exist yet (placeholders visible); X version under 280 characters; no GitLab link; handoff seams table names what is code and what is prose; X4 blocks in `docs/_pending/WS-CONTENT.md` | blocked (round 1 after two fix rounds: one major remains, the handoff's Langfuse sentence; R2-CONTENT) |
| WS-DOCS: doc-apply pass (runs alone after the five streams), consuming every `docs/_pending/*.md` including the M1 leftovers | WS-DOCS (orchestrator-run, one agent) | `KEY_METRICS.md`, `ARCHITECTURE.md`, `AUDIT.md`, `README.md`, `README_AGENT_FORGE.md`, `SETUP.md`, `AI_COST_ANALYSIS.md`, `USERS.md`, `docs/SUBMISSION_CHECKLIST.md`, `docs/REQUIREMENTS_TRACEABILITY.md`, `docs/PROJECT_PLAN.md`, `docs/adr/**`, `docs/operations/**`, `docs/PRIOR_COHORT_LESSONS.md`; this status file (orchestrator only) | Every `EVIDENCE:` line re-verified before its block is applied; `docs/_pending/` empty afterward; conflicts between deltas or with the code reported, not resolved silently | blocked (round 1: 96 blocks applied, 3 skipped as already applied, 3 merges; the read-only check found two majors, R2-DOCS) |
| R2-AGENT: production-branch test for the `_observation` re-raise (only the tracer-off branch is tested); reword "a rebuild cannot resolve anything newer" to "no runtime dependency can resolve newer" (setuptools is fetched at build time); align `.gitlab-ci.yml` `test:evals-live` install with the lock; ADR-0005 delta noting that checkpoints written before 2026-09-17 carry the `token` channel | WS-AGENT | as WS-AGENT | `cd agent && .venv/bin/python -m pytest -q` green; new test fakes `langfuse.get_client` and asserts `ModelError` propagates and `__exit__` saw it | verified (`9a13d7b`, `8615162`: 96 tests pass, new test exercises the tracer-ON branch, wording corrected in three places plus the lock header) |
| R2-EVAL: fix `_cost_usd` (prices uncached input at $0 whenever `cache_read_tokens > input_tokens`; the API already reports `input_tokens` uncached) and footnote in `evals/README.md` that reports before the fix under-count by about 10%; add the false-positive surface of the wording regexes to `ISO-FRESH-REPEAT-001` `risk:`; add `--out-dir` to `evals/run.py` so gate runs stop writing into the repo | WS-EVAL | as WS-EVAL | unit probe of `_cost_usd` on a4a5856 usage shows input priced at the base rate; `evals/run.py --offline-only --out-dir <scratch>` writes nothing under `evals/results/`; deltas for AI_COST_ANALYSIS.md and KEY_METRICS.md where they present the clamp as a fact | verified (`fbea630`: orchestrator recomputed the a4a5856 aggregate independently, old \$0.0134 -> new \$0.0147 per turn, ~9.9% — matches the stream's figure; `--out-dir` confirmed to leave `evals/results/` untouched) |
| R2-LOAD: the 90 s timeout is per SSE frame, not per turn; drop the non-existent `library/authentication/` citation; make `run_virtual_user` never raise (`.get()` on the ticket, broad except recording `failed_at`); README uses `kill -TERM` for the sampler | WS-LOAD | as WS-LOAD | `pytest -q evals/load/test_run_load.py` green; `grep -n 'library/authentication' evals/load/` empty | verified (`3d090e5`: 46 offline tests pass, up from 43; `library/authentication` grep empty; `kill -TERM` in the README) |
| R2-INFRA: "7 model-backed of the 14 golden cases"; add `curl` to the cloud-init package list (start.sh depends on it); `--exclude=build` on the deploy.sh agent tarball; delta for `docs/operations/alerts.md` that the readiness page rule now includes tracer reachability | WS-INFRA | as WS-INFRA | `PUBLIC_HOSTNAME=x TLS_EMAIL=y docker compose --env-file /dev/null config --quiet` exit 0; `bash -n` on every touched script | verified (`2eb4573`: compose config exit 0, all four scripts `bash -n` clean, `curl` in cloud-init, `--exclude=build` in deploy.sh) |
| R2-CONTENT: `docs/WEEK2_HANDOFF.md` Langfuse residual (the A3 trace scores go through `/api/public/ingestion`, so an SDK bump before 2026-11-16 is required, not a watch item); seams row evidence `turns.py:26` not `common.py:15-16`; `docs/INTERVIEW_NOTES.md` load-test sentences (:198, :282), the monitoring bullet's qualifier, and the `digitalocean.md` pointers by heading instead of line | WS-CONTENT | as WS-CONTENT | `grep -n 'on neither' docs/WEEK2_HANDOFF.md` empty; `grep -n 'no load test\|not in the tree' docs/INTERVIEW_NOTES.md` empty | verified (`e734e04`: both greps empty; orchestrator independently re-derived the SDK call chain to `/api/public/ingestion` and it matches the stream's citations exactly) |
| R2-DOCS: apply the twelve round-2 delta blocks (WS-AGENT x4, WS-EVAL x6, WS-INFRA x1, WS-CONTENT x1) to `AI_COST_ANALYSIS.md`, `KEY_METRICS.md`, `README_AGENT_FORGE.md`, `docs/adr/0004`, `docs/adr/0005`, `docs/operations/alerts.md`, `docs/operations/langfuse-dashboard.md` | WS-DOCS | as WS-DOCS | every ANCHOR/CURRENT verified against the tree before applying; `docs/_pending/` empty afterward; no stale echo of the round-1/round-2 findings survives elsewhere (`grep -rn` swept for each) | verified (orchestrator applied all 12 blocks directly — see below; folded into the owning streams' commits since each touched its own doc). `README_AGENT_FORGE.md` :268/:141/:263 and the `docs/FINAL_SUBMISSION_TODOS.md` State lines were on WS-AGENT's own delta list (README_AGENT_FORGE test counts, already applied by WS-AGENT R2-A1) and were not separately re-flagged this round; `ARCHITECTURE.md:587` was not in any round-2 delta stream's list, so the orchestrator fixed it directly, verified against `agent/app/graph/nodes.py` (`classify` routes follow-ups to `plan`, which calls `rt.model.plan(...)` before any gateway call; only a UC-01 first turn skips the model entirely) |

## Deferred

- **E3 `ISO-TWO-USERS-001`** — the harness has no `as_user:` step (`evals/run.py:407-452`
  builds one `Session` per case from `case["user"]`); a second concurrent session means
  dual ticket and conversation tracking through `check()`, past the plan's two-hour bar.
  Target: one line under `evals/README.md` "Not automated in Week 1" (WS-EVAL) and an
  `ARCHITECTURE.md` delta keeping "two users on the same patient" listed as untested.
- **X3 `docs/WEEK2_HANDOFF.md`** ships as the one-page stub the plan's cut rule 4
  allows (read-first order, seams table, residuals, the two deferred experiments by
  name with the baseline-change-compare protocol). The long form is Week 2 work.
- **A7 breaker test** is in scope; it is the first A7 item to drop if the clock bites,
  per cut rule 2. The checkpoint-content test is never cut.
- **M1 leftover delta** in `docs/_pending/WS-CONTENT.md` (an "Automated coverage today"
  sentence after the ARCHITECTURE.md failure matrix) was never applied by an M1
  doc-apply pass. It is recorded here because `docs/_pending/` is gitignored
  (`.gitignore:80`); the M2 doc-apply pass consumes it.
- **Owner steps M3 needs that the plan does not assign** (found by the completeness pass; recorded here, the plan's M3 text is not edited without the owner): (1) build the two Langfuse panels (average of trace score `verification_passed`, average of `turn_error`, over `copilot.turn` traces) before M3 gate item 2 can be checked — the scores exist on every trace after the deploy, the panels can only be made in the Langfuse UI; (2) after the deploy, open a conversation checkpointed before it (the deployed volume's checkpoints carry a `token` channel that `TurnState` no longer declares) or confirm the volume policy recreates `agent_state`; (3) confirm `tracer: reachable` and one alerts heartbeat — `/ready` now depends on egress to the Langfuse host every 30 s cache window and the alerts service pages after two minutes of `/ready` failing, so a tracer outage pages while the app works.
- **Human-gated, not attempted in M2:** the WS-LOAD `--users 2` smoke, the
  `--golden-only` live run, every deploy, rehearsal, and backup execution.
- **`docs/audit/scripts/droplet-stats.sh` and `docs/deployment/digitalocean.md`'s I2/I3 remote behaviour** — verified only with `bash -n`, `docker compose config --quiet` (placeholder env), and read-only greps in both M2 rounds; MariaDB volume re-init timing, the backup archive's actual restore, and the alerts service's real heartbeat are first exercised at M3/M4.

## Contradictions found in the inputs

1. **M1 doc-apply pass did not run as specified.** `docs/_pending/WS-DOCS.md` and
   `docs/_pending/WS-CONTENT.md` survived M1. Commit `aa4d19a` applied the two
   `docs/deployment/digitalocean.md` blocks and satisfied the `evals/README.md:14`
   block in a different form (an appended note at `:15-16`, the original line kept).
   Only the WS-CONTENT `ARCHITECTURE.md` block is unapplied.
2. **A3 names a function that does not exist.** The plan says to call
   `score_trace("verification_passed")` and `score_trace("turn_error")` in
   `finish_turn_trace`; nothing in `agent/` defines `score_trace`
   (`agent/app/telemetry.py` has `finish_turn_trace`, `generation`, `tool_observation`,
   `record_tool_result`). It must be written, not just called.
3. **E2's "deterministic" alternative is still model recall.** `kind: note_vs_list` is
   matchable (`evals/run.py:182-184`), but the value is prompt-instructed
   (`agent/app/model.py:43`) into a free-string field
   (`agent/app/contracts/turns.py:47`). Swapping the text regex for the kind field
   trades one model-produced assertion for another. The choice is to be made from
   `facts.kind` on the conflict claim across the recorded run JSONs, not from the
   plan's framing.
4. **A1 is two changes.** `agent/app/readiness.py:96-103` awaits its checks
   sequentially; "gather the three async checks" is a second change beyond making
   `check_tracer` async. Both are in scope.
5. **Isolation-invariant count.** The plan's M1 text says "two isolation invariants
   untested"; after M1, `ARCHITECTURE.md:498-503` correctly lists three (fresh-conversation
   repeat, two users on one patient, checkpoint content). A7's checkpoint-content test and
   E3's `ISO-FRESH-REPEAT-001` bring that to one, which stays deferred (above).

6. **The M2 gate line `cd infra/digitalocean/runtime && docker compose config --quiet` cannot pass verbatim in a clean checkout**: `compose.yaml` uses `${PUBLIC_HOSTNAME:?}` / `${TLS_EMAIL:?}` (pre-existing at HEAD) and the runtime `.env` is gitignored. It passes as `PUBLIC_HOSTNAME=x TLS_EMAIL=y docker compose --env-file /dev/null config --quiet`, the form `.gitlab-ci.yml` `lint:compose` uses. The plan's gate line is corrected to that form by the orchestrator.
7. **The delegation token was in the checkpoint at HEAD `66a6711`**: `api._turn_input` put `token` into `TurnState` and LangGraph writes every channel, so `checkpoints.sqlite` blobs held the token bytes, contradicting ADR-0005's "PHI at rest is limited to claim text". Fixed by WS-AGENT (per-turn token cache in `state_store`, `token` removed from `TurnState`, asserted by `test_checkpoint_holds_no_note_body_record_shape_or_token`). Pre-deploy checkpoints on the Droplet still carry it (Deferred, owner step 2).
8. **Every telemetry context manager at HEAD converted a body exception into `RuntimeError('generator didn't stop after throw()')`**, so a `ModelError` from `AnthropicModel._guarded` bypassed the graph's `except ModelError` fallback and would have been a 500, not the deterministic fallback ADR-0004 and the failure matrix promise. `MODEL-OUTAGE-001` never caught it because the fault header short-circuits before the span. Fixed by WS-AGENT with a regression test (tracer-off branch only; R2-AGENT adds the production branch).
9. **A5's "reason enum in `contracts/tools.py`" did not exist** (`ToolResponse.reason` was `str | None`); WS-AGENT defined `TOOL_REASONS` from the literals the gateway, `gateway_client.py` and `nodes.py` emit. Wire contract unchanged (`export --check` passes).
10. **The checkpoint holds more than ARCHITECTURE.md:481 said**: the current turn's unverified `raw_claims`/`raw_summary`/`raw_suggestions`, and `tool_calls` as `[tool, params]` with no hash and no source ids. Doc corrected; code unchanged by decision (moving it to the per-turn cache touches every node).
11. **ARCHITECTURE.md and the traceability matrix claimed the Langfuse dashboard shows "verification outcome"**; none of the nine panels does. Docs now say the two scores exist per trace and the panels are "to build" (Deferred, owner step 1).
12. **`evals/run.py` `_cost_usd` under-counts every turn by about 10%**: it subtracts `cache_read_tokens` from `input_tokens` and clamps at zero, but `agent/app/model.py:_usage_of` already stores the API's uncached `input_tokens` with cache reads separate, so uncached input is priced at $0 whenever cache reads exceed it (every eval turn). Contradicts `run.py`'s own pricing comment and AI_COST_ANALYSIS.md Part B. R2-EVAL fixes it; the $0.0127 figures in the docs remain true of the reports that printed them.
13. **WS-EVAL's round-0 text called `turn_type: uc01_first` a deterministic isolation proof**; `nodes.py classify` types any UC-01 question `uc01_first` with or without history, so only `turns_max: 0` discriminates leaked history. Corrected before commit. Two round-0 cost figures in the AI_COST_ANALYSIS.md delta were also wrong ($0.0123 → $0.0121 floor; the 1,306-token report is `2026-09-16T041843Z`) and corrected.
14. **`cost_gate` compares at the scorecard's four-decimal precision**, so the effective boundaries sit at the fifth decimal ($0.02235 warn, $0.04465 FAIL). Documented in the docstring; no behaviour change.
15. **Plan I3's restore order cannot work on a database volume initialised under different secrets**: the MariaDB image reads `MARIADB_*_PASSWORD_FILE` only into an empty datadir, so `restore.sh` removes the database container and its volume and re-initialises from the restored secrets before loading the dump. Destructive by design, documented, no automatic pre-restore backup.
16. **Plan I4's rehearsal needs a second Terraform variable**: `main.tf` registers `digitalocean_ssh_key` from `var.ssh_public_key_path` and DigitalOcean rejects a duplicate fingerprint, so the runbook also passes the throwaway key path.
17. **`docs/deployment/digitalocean.md` said the host runs a tree identical to HEAD**; after M2 `agent/` and `infra/digitalocean/runtime/` differ from tag `week1` while the host still runs `e1dd331`. Fixed by WS-INFRA.
18. **`docs/operations/alerts.md` documented a cron line as the scheduler and "exit code 2 lets cron escalate"**; the compose `alerts` service loops under `--interval 300` and never exits, and a cron line beside it would corrupt the shared state file's rate math. Replaced by the service and webhook/log-watch escalation.
19. **Nothing in the repo installs `curl` on the Droplet** (`cloud-init.yaml.tftpl` installs only Docker packages) while the new `start.sh` livez gate depends on it; Ubuntu images ship it, unverified until M3 (R2-INFRA adds it to the package list).
20. **ARCHITECTURE.md's failure matrix row "Gateway unreachable → turn ends with `dependency_unavailable`" was false**: `gateway_client.py` returns per-tool `unavailable`/`transport_error` and the turn continues; `dependency_unavailable` is emitted only on the 45 s wall-clock timeout. Reworded by the doc-apply pass; its "the model is not invoked" is itself false for follow-ups (the planning call runs before retrieval) — R2-DOCS.
21. **`agent/requirements.lock` pins `langchain==1.4.1`, `langfuse==4.15.4` (container, Python 3.12.14) while `agent/.venv` has 1.4.0 / 4.15.3 on Python 3.13.2.** Both pass the 91 tests; the lock is the version of record. `.gitlab-ci.yml:71` says the live suite costs about $0.55; the a4a5856 report records $0.51.
22. **`docs/WEEK2_HANDOFF.md` says the project uses neither Langfuse endpoint family removed on 2026-11-16**; the A3 trace scores are posted through `/api/public/ingestion` (SDK chain: `LangfuseSpan.score_trace` → `create_score` → score ingestion consumer → `POST /api/public/ingestion`). Unfixed after two fix rounds; R2-CONTENT.
23. **The doc-apply pass counted three offline-only acceptance-run reports** (`evals/results/2026-09-18T*-66a6711.*`, zero turns, $0) in six documents. The orchestrator deleted the six untracked files (verification byproducts, would have falsified the eleven/ten counts M1 established) and reverted the five sentences before committing. `RESULTS_DIR` is hard-coded, so every `--offline-only` gate run writes a report pair into the repo; R2-EVAL adds `--out-dir`.
24. **Retracted, not a contradiction**: WS-LOAD's round-0 claim that `evals/run.py` line numbers were off at HEAD was false (Session :63-129, start :91, mint :97, turn :102, history :121 at `66a6711`) and must not be carried forward.
25. **`docs/FINAL_PUSH_PLAN.md`'s M3 step 4 ("Agent smoke-tests the load driver at `--users 2`") contradicts `evals/load/README.md`** ("Running either tool against the deployment is M4 and human-gated... not something a CI job or an agent starts"). Resolved in favor of the README (more specific, more recent, and consistent with invariant 5 on model spend): the orchestrator did not run the load driver itself and asked the owner to; the owner ran it.

## Completeness

- **2026-09-17, round 1** (workflow `wf_4c04f4e6-0eb`: 30 agents, 0 errors; five implement, two verifier lenses per stream, fix rounds for WS-EVAL, WS-LOAD, WS-CONTENT ×2, doc-apply, read-only doc check, completeness critic). Committed as `16a5d34` (agent), `a6d2f9c` (evals), `125d7a9` (load), `58f4098` (infra), `a4aedae` (content), `7197ed6` (doc-apply). Gate as run by the critic: 91 tests pass, 9 schemas checked, 9/9 offline cases, `docs/_pending/` empty; compose line fails verbatim and passes in placeholder form (contradiction 6). Gaps found: the R2-* rows above, plus three M3 owner steps (Deferred). Tree hygiene done by the orchestrator before committing: `agent/build/` deleted and gitignored (a verifier's in-tree `pip install` created it), six zero-turn `evals/results` files deleted, five count sentences reverted. Two unowned docs edited by the doc-apply pass because blocks targeted them (`docs/GLOSSARY.md`: case count and NOT CONFIGURED definition; `docs/FINAL_SUBMISSION_TODOS.md`: the cost-gate item ticked with evidence); `.gitignore` gained `agent/build/` in the WS-AGENT commit. What no verifier could confirm (first exercised at M3/M4): A6's container facts beyond the host-side re-derivation, A1 `tracer: reachable`, A3 scores arriving in Langfuse, I1 heartbeat, I2 gates, I3/I4 on a host, L1's live handshake, E1 on a run with model-backed turns, E3 live.
- **2026-09-17/18, round 2** (workflow `wf_cf49b565-e39`: Fix phase completed for all five streams — six commits' worth of code and delta blocks written to disk — then the user killed the run mid-Verify because the model (Fable 5.1) was consuming tokens too fast. Nothing was lost: every Fix-phase agent had already returned and written its files and `docs/_pending/*.md`. Rather than relaunch a second multi-agent workflow, the orchestrator (now Sonnet 5, ultracode off) verified every stream's round-2 work directly — re-ran each stream's acceptance commands, independently re-derived the two substantive claims (the `_cost_usd` arithmetic: old \$0.0134/turn -> new \$0.0147/turn on the a4a5856 aggregate, ~9.9%, matching WS-EVAL's figure; the Langfuse SDK score-ingestion call chain to `/api/public/ingestion`, matching WS-CONTENT's citations line for line), confirmed the ARCHITECTURE.md follow-up-planning-call claim against `agent/app/graph/nodes.py` and `build.py` directly, then applied all twelve round-2 delta blocks by hand (ANCHOR/CURRENT verified against the tree before each REPLACE WITH) and swept for stale echoes elsewhere. Committed as `9a13d7b` (agent), `fbea630` (evals: cost gate + `--out-dir`), `3d090e5` (evals: load driver), `2eb4573` (infra), `e734e04` (content), `8615162` (agent: lock header follow-up). One gap the round-2 streams' own task lists did not cover — `ARCHITECTURE.md:587` still said "the model is not invoked" on a gateway-unreachable turn, false for follow-ups — was found and fixed directly against the code, not delegated. Final M2 gate (all five commands from `docs/FINAL_PUSH_PLAN.md`, run by the orchestrator): 96 agent tests pass (up from 91), 9 schemas checked, `evals/run.py --offline-only --out-dir <scratch>` 9/9 with `evals/results/` left untouched, `docker compose config --quiet` (placeholder env) exit 0, full diff inside the M2 ownership map, `docs/_pending/` empty. No blocker or major finding remains open. **MILESTONE M2 is done.**

- **2026-09-17/18, post-M2** (owner decision, not delegated planning): the owner merged `week1-final-push` into `main` early via [!2](https://labs.gauntletai.com/andrebatista/andrebatista-openemr-base-clean/-/merge_requests/2) (`81dade8`) — the early submission is already graded, so `main` is now the working branch and the rest of this plan's "single MR at M5" branch model is superseded (`docs/FINAL_PUSH_PLAN.md`'s working-branch section and invariant 4 both carry a 2026-09-17 amendment note explaining this). The owner then asked for CI to deploy `main` to `137.184.4.22` on every push, automatically, no click — an explicit, informed override of invariant 4 for that one script only; `tf.sh apply`, `destroy.sh`, `smoke-cycle.sh`, and `push-secrets.sh` stay human-run. Built: a `deploy` stage (`.gitlab-ci.yml` `deploy:production`, a dedicated `DEPLOY_SSH_PRIVATE_KEY` protected file-type CI/CD variable, never the operator's own key) and a `verify` stage (`verify:smoke`, automatic; `test:evals-live` moved here too, manual, now tests the commit actually deployed instead of whatever predated it). Validated against GitLab's CI lint endpoint before committing (`d7fd6b3`).

  Two steps only the owner could do (the auto-mode classifier correctly blocked both when the agent tried: a production read, then a production write categorized "Unauthorized Persistence") were handed off in chat and confirmed done by the owner: appending the CI public key to the `deployer` user's `authorized_keys` on the Droplet, and setting the real `TLS_EMAIL` CI/CD variable.

  Running `tf.sh apply` to open the firewall for the CI runner's IP (`134.199.216.98/32`, the owner's explicit instruction, distinct from the deploy-on-push decision) surfaced a real near-miss: `plan` showed `1 to destroy` — `digitalocean_droplet.app`'s `user_data` is DigitalOcean's ForceNew field, and round 2's own `2eb4573` (adding `curl` to `cloud-init.yaml.tftpl`, a change verified at the time only by checking `docker compose config --quiet`, not by planning against the already-provisioned resource) had already changed its rendered content enough to make *any* future apply plan to destroy and recreate the live, already-seeded, publicly-reachable Droplet. Not applied. Fixed durably with `lifecycle { ignore_changes = [user_data] }` on the droplet resource (`59ed1ae`) rather than just reverting the line, so a future `cloud-init.yaml.tftpl` edit (legitimately useful for the M4 rehearsal's fresh droplet) can never again silently threaten this one; recreating it now needs `terraform apply -replace`, a deliberate act. Re-planned clean (`0 to add, 1 to change, 0 to destroy`), applied, re-planned again after (`No changes`), host confirmed live before and after via the public `/meta/health/livez` endpoint (not SSH).

  Pipeline `24056` (commit `59ed1ae`) then ran end to end for real: `deploy:production` succeeded, `verify:smoke` succeeded, and an independent `curl` from outside CI against `/copilot-api/ready` confirmed `"status": "ready"` with every dependency `ok: true` — including `"tracer": "reachable"`, the A1 fix from M2 proving out live for the first time. This is the M3 STOP gate's core signal (a verified single deploy) reached by a different mechanism than the plan describes (continuous automated deploy rather than one manual M3 push); it is not the full M3 checklist (`docs/FINAL_PUSH_PLAN.md`'s Bruno collection run, `--golden-only`, the alerts heartbeat check, and the owner's own panel/citation click-through remain unconfirmed) and M4 (load tests, rehearsal, backups, the egress decision) has not started.

  Pushing to `origin` (the public GitHub fork) was raised and the owner said to drop it for now; `origin/main` remains behind `gitlab/main`.

- **2026-09-18, M3 agent-run items**: three of the four agent-doable M3 verification commands now ran clean against `137.184.4.22`. Bruno (`docs/api-collection`, `bru run --env deployed`): 21/21 requests, 14/14 tests, 41/41 assertions — first attempt showed 16/21 failures (login returned 200 instead of 302, i.e. rejected) because of a shell scoping bug in the orchestrator's own command (`VAR=val cmd --arg="$VAR"` expands `$VAR` before the prefix assignment applies, so `DEMO_PASSWORD` was empty on every request); exporting the variable first fixed it, not a product bug. `evals/run.py --golden-only` against prod: 14/14 (gate table correctly shows NOT RUN across the board — a filtered run isn't judged against the full-manifest gate table by design), report `evals/results/2026-09-18T070752Z-5c3c399.md`. The load driver's `--users 2` smoke (contradiction 25) was run by the owner, not the orchestrator: 2/2 VUs, 4 turns, 0 errors (0.0%), 100% complete status share, turn p95 21937 ms, report `evals/load/results/2026-09-18T071520Z-5c3c399.md`. Remaining M3 items, all owner-only: the alerts heartbeat over SSH, the panel/citation click-through as `audit-physician`, and confirming the two new Langfuse panels draw data.
