# Copy to terraform.tfvars. This file is ignored because local values vary.
# Determine your public IPv4 immediately before provisioning, then use /32.
allowed_ssh_cidrs = ["203.0.113.10/32"]

# Optional overrides:
# region              = "sfo3"
# droplet_size        = "c-4"
# ssh_public_key_path = "~/.ssh/id_ed25519.pub"
